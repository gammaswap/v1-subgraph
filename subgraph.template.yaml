specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  - name: GammaFactory
    kind: ethereum/contract
    network: {{network}}
    source:
      address: "{{factory}}"
      abi: Factory
      startBlock: {{startBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/mappings/factory.ts
      entities:
        - GammaPool
      abis:
        - name: Factory
          file: ./abis/factory.json
      eventHandlers:
        - event: PoolCreated(indexed address,indexed address,indexed uint16,address,address[],uint256)
          handler: handlePoolCreate
  - name: PositionManager
    kind: ethereum/contract
    network: {{network}}
    source:
      address: "{{positionManager}}"
      abi: PositionManager
      startBlock: {{startBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/mappings/positionManager.ts
      entities:
        - Loan
      abis:
        - name: PositionManager
          file: ./abis/positionManager.json
      eventHandlers:
        - event: CreateLoan(indexed address,indexed address,uint256)
          handler: handleLoanCreateFromPositionManager
templates:
  - name: GammaPool
    kind: ethereum/contract
    network: {{network}}
    source:
      abi: Pool
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/mappings/pool.ts
      entities:
        - GammaPool
        - Loan
        - Position
        - Liquidation
        - Account
      abis:
        - name: Pool
          file: ./abis/pool.json
      eventHandlers:
        - event: PoolUpdated(uint256,uint256,uint48,uint96,uint256,uint128,uint128,uint128[],indexed uint8)
          handler: handlePoolUpdate
        - event: LoanCreated(indexed address,uint256)
          handler: handleLoanCreate
        - event: LoanUpdated(indexed uint256,uint128[],uint128,uint128,uint256,uint96,indexed uint8)
          handler: handleLoanUpdate
        - event: Liquidation(indexed uint256,uint128,uint128,uint128,uint8,uint256[])
          handler: handleLiquidation
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handleVaultTokenTransfer